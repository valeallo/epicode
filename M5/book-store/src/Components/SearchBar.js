import React from 'react'

// 4. Crea un campo di testo controllato all’interno del componente BookList;
//  il componente deve utilizzarlo come filtro e mostrare solamente i libri che contengono nel titolo il testo cercato. 
// (Suggerimento: salva il contenuto del campo di testo nello stato del componente e utilizzalo per filtrare il tuo .map()).


const SearchBar = () => {
  return (
    <input type="text"/>
  )
}

export default SearchBar